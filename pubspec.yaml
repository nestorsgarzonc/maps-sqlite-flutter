name: sqlpeoyext
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  barcode_scan: ^1.0.0
  sqflite: ^1.2.0
  path_provider: ^1.5.1
  url_launcher: ^5.4.1
  google_fonts: ^0.2.0
  flutter_map: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
